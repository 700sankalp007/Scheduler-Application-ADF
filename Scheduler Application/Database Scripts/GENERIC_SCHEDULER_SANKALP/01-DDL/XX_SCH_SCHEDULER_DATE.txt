CREATE TABLE XX_SCH_SCHEDULER_DATE 
   (	"SCHEDULER_DATE_ID" NUMBER, 
	"SERVICE_TYPE" VARCHAR2(100 BYTE), 
	"SCHEDULER_FLAG" VARCHAR2(1 BYTE), 
	"SCHEDULER_DATE" TIMESTAMP (6), 
	"CREATED_BY" VARCHAR2(255 BYTE), 
	"UPDATED_BY" VARCHAR2(255 BYTE), 
	"UPDATION_DATE" DATE, 
	"CREATION_DATE" DATE, 
	"SCHEDULER_ID" NUMBER
   );

ALTER TABLE XX_SCH_SCHEDULER_DATE MODIFY ("SCHEDULER_DATE_ID" NOT NULL ENABLE);